# Denta Doctor App

React Native + Expo mobile app for doctors.

```bash
cd apps/doctor-app
npm run start
```

Metro: http://localhost:8082

This is **not** the Clinic CRM. Do not open Expo Web and treat it as Clinic CRM.
Clinic CRM is Next.js at `apps/clinic-web` → http://localhost:3000
