# Denta Client App

React Native + Expo mobile app for patients.

```bash
cd apps/client-app
npm run start
```

Metro: http://localhost:8081

This is **not** the Clinic CRM. Clinic CRM is Next.js at `apps/clinic-web` (port 3000).
